# Email configuration for sending call logs
# Replace 'your_office365_password_here' with the actual Office365 password for nvenu@solifetec.com
EMAIL_PASSWORD=Password10
# Instructions:
# 1. Copy this file to .env
# 2. Replace the password with the actual Office365 password
# 3. For Office365, you can use either:
#    - Regular password (if MFA is not enabled)
#    - App password (if MFA is enabled - create in Office365 security settings)