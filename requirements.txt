# RingCentral Reports - Required Python Packages

# RingCentral API SDK (REQUIRED)
ringcentral>=0.7.14

# Excel file creation and manipulation (REQUIRED)
openpyxl>=3.1.2

# Environment variable management (REQUIRED)
python-dotenv>=1.0.0

# HTTP requests (usually installed with ringcentral, but listed for clarity)
requests>=2.31.0

# Date and time utilities (usually built-in, but good to have)
python-dateutil>=2.8.2